# Trigger fixtures for the `lovebuilder` skill.
# Real prompts get filled in during Phase 1 when lovebuilder is authored.
# Minimums required at end of Phase 1: >=5 should_fire, >=5 should_skip, >=2 should_disambiguate.

skill: lovebuilder
version: 0.1.0

should_fire:
  # Example (to be replaced with real fixtures in Phase 1):
  # - prompt: "scaffold the project"
  #   expected_artifacts: ["main.lua", "conf.lua", ".love-version"]

should_skip:
  # - prompt: "how does love.physics work?"
  #   correct_skill: lovedoc

should_disambiguate:
  # - prompt: "I want a dash"
  #   expected_clarification_contains: ["redirect", "ability"]
